Files in this directory can be accessed easily though the methods exposed in ResourcesHelper module.

e.g. `resources_file('images', 'foo.jpg')` would return a File object representing a file stored here.
